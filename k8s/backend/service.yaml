apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: ollamahub
  labels:
    app: backend-deployment-label
spec:
  type: ClusterIP
  selector:
    app: backend-deployment-label 
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000