apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: ollamahub
  labels:
    app: postgres-service
spec:
  selector:
    app: postgress-deployment
  ports:
  - port: 5432
    targetPort: 5432
